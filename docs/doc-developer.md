## Instructions for developers

To view the MkDocs site on one's local machine with live reloading, one can run `make view-docs` from the root of this repository. 

Alternatively, one can manually run the `mkdocs serve --livereload` command here.