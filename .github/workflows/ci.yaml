on:
  workflow_dispatch:
  push:
    branches-ignore:
      - main
    paths:
      - nodekit/**/*.py
      - examples/**/*.py
      - tests/**/*.py
      - .github/workflows/ci.yaml
      - pyproject.toml
      - Makefile
      - uv.lock
jobs:
  NodeKit-CI:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository code
        uses: actions/checkout@v5
      - name: Setup Python
        uses: actions/setup-python@v6
        with:
          python-version: '3.12'
      - name: Install uv
        run: |
          pip3 install uv
          uv venv
          source .venv/bin/activate
      - name: Compile nodekit-rs
        run: |
          make build-rs
      - name: Install nodekit
        run: |
          uv sync --group dev --locked
      - name: Check
        run: |
          make check
      - name: Test
        run: |
          make test
