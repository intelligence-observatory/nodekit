

export class PointerStream {
    // Listens for pointer events on a given div.
    // Normalizes them to Board coordinates and emits them to subscribers.
    constructor(
        target: HTMLDivElement,
    ){


    }
    subscribe(){

    }
}