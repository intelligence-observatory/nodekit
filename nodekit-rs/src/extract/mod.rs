mod extractor;

use std::path::Path;
use ffmpeg_next::{Error, Stream};
use ffmpeg_next::format::{input, context::Input};
use ffmpeg_next::media::Type;
use ffmpeg_next::codec::context::Context;
use ffmpeg_next::decoder::Decoder;

fn lol() {
    
}